FROM nhost/hasura-backend-plus:latest

ENV AUTH_ACTIVE=true
ENV AUTH_LOCAL_ACTIVE=true
# ENV USER_FIELDS=
ENV USER_REGISTRATION_AUTO_ACTIVE=true
ENV REFRESH_TOKEN_EXPIRES=43200
ENV JWT_TOKEN_EXPIRES=15

ENV STORAGE_ACTIVE=false
# ENV S3_ACCESS_KEY_ID=<access>
# ENV S3_SECRET_ACCESS_KEY=<secret>
# ENV S3_ENDPOINT=<endpoint>
# ENV S3_BUCKET=<bucket>

CMD ["node", "src/index.js"]
